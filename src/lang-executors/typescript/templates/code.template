export default async function main(): Promise<
    {
        content: any;
        blockIndex: number;
    }[]
> {
const __arr: {
    content: any;
    blockIndex: number;
}[] = [];
const __consume = (blockIndex: number, content: any) =>
    __arr.push({ content, blockIndex });
{{##1##}}
return __arr;
}
