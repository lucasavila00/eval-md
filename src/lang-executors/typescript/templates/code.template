import * as util from "util";

export default async function main(): Promise<
    {
        content: any;
        blockIndex: number;
    }[]
> {
const __arr: {
    content: any;
    blockIndex: number;
}[] = [];
let __dnt = false;
const __consume = (
    outLanguage: string,
    blockIndex: number,
    content: any
) => {
    if (outLanguage === "jsonjs") {
        __arr.push({ content: util.inspect(content), blockIndex });
    } else if (outLanguage === "error") {
        __arr.push({ content: String(content), blockIndex });
    } else {
        __arr.push({ content: JSON.stringify(content), blockIndex });
    }
};
{{##0##}}
// prevent "is declared but its value is never read."
if (Math.log(1) > 0) {
    String(__consume);
    String(__meta);
    String(util);
    String(__dnt);
}
return __arr;
}
