-   [ ] ts compiler

    -   [ ] scaffold output gen folder
            // hoist imports
            // export default 1 gen
            // index runs and serializes it, output to stdout

-   [ ] cleanup the since
-   [ ] attribute docs-ts
